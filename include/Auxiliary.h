#pragma once
#include <iostream>
#include <vector>
#include <sstream>
#include <string>

cout << "hi"
class Auxiliary{
    public:
        static std::vector<std::string> parseArguments(const std::string& line);
};
